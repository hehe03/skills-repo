{
  "rules": [
    {
      "id": "labeled_bad_high_step_count",
      "layer": "labeled",
      "label": "badcase",
      "weight": 0.35,
      "description": "Labeled badcase train traces have higher step_count than goodcase traces.",
      "all": [
        {
          "feature": "step_count",
          "op": ">=",
          "value": 8
        }
      ]
    },
    {
      "id": "labeled_bad_high_repeated_action_count",
      "layer": "labeled",
      "label": "badcase",
      "weight": 0.35,
      "description": "Labeled badcase train traces have higher repeated_action_count than goodcase traces.",
      "all": [
        {
          "feature": "repeated_action_count",
          "op": ">=",
          "value": 4
        }
      ]
    },
    {
      "id": "labeled_bad_high_max_consecutive_same_action",
      "layer": "labeled",
      "label": "badcase",
      "weight": 0.35,
      "description": "Labeled badcase train traces have higher max_consecutive_same_action than goodcase traces.",
      "all": [
        {
          "feature": "max_consecutive_same_action",
          "op": ">=",
          "value": 3
        }
      ]
    }
  ],
  "note": "Generated by scripts/rule_generation.py. Deterministic experiment rules; no manual review is required before evaluation."
}
